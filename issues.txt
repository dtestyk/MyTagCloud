default setup is show(checkbox),
but on tab open hidden

show tag cloud too slow: youtube 10s
not related with last changes show cloud bound show

get tags by url on page side
not because plan to request from other nodes
if on page side need reconnect with every page


url params order may change

auto tags surfing:
  deactivate tag by timeout, 
  ignore tag by timeout
  random tag select
  proportional to n

auto tags surfing:
  перебор подмножества тегов
  счет в коде Грея
  
hashchange event on window


user tag address
wiki/Расстояние_Хэмминга

теги:
  персональные
  коллективные

теги:
  новые
  популярные
  

поиск
  метод градиентного спуска с изменением одной координаты в аттрактор
  параметр оптимизации: количество посещений, время посещения


добавить в данные о странице
  количество посещений n
  суммарное время посещения t_sum
  возм. t(n):
    время пребывания t(p=0.5) - момент наибольшей неопределенности
    случ. время пребывание t(p=rnd) моделирующее пользователя(наим. искажение статистики)
  возм. n(t): вероятность ухода в определенный момент p(t)

автосерфинг по тегам(показанным)
  кнопка спуск в аттрактор(локальный минимум)
  кнопка перебор подмножеств тегов(циклическим(например, зеркальным) кодом Грея)
  кнопка случайный автосерфинг
кнопка остановить автосерфинг

флажек показать теги
  зеленый: новые
  синий: персональные популярные
  черный: коллективные популярные(те, что не вошли в персональные)


эквивокация: одно слово - разные значения

возможность добавить слово со страницы
в качестве тега
нормализация, выбор синонима

возможность менять suggestion
при наведении на слово на странице