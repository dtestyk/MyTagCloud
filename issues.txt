default setup is show(checkbox),
but on tab open hidden

show tag cloud too slow: youtube 10s
not related with last changes show cloud bound show

get tags by url on page side
not because plan to request from other nodes
if on page side need reconnect with every page


url params order may change

auto tags surfing:
  deactivate tag by timeout, 
  ignore tag by timeout
  random tag select
  proportional to n

auto tags surfing:
  перебор подмножества тегов
  счет в коде √ре€
  
hashchange event on window


user tag address
wiki/–ассто€ние_’эмминга

теги:
  персональные
  коллективные

теги:
  новые
  попул€рные
  

поиск
  метод градиентного спуска с изменением одной координаты в аттрактор
  параметр оптимизации: количество посещений, врем€ посещени€


добавить в данные о странице
  количество посещений n
  суммарное врем€ посещени€ t_sum
  возм. t(n):
    врем€ пребывани€ t(p=0.5) - момент наибольшей неопределенности
    случ. врем€ пребывание t(p=rnd) моделирующее пользовател€(наим. искажение статистики)
  возм. n(t): веро€тность ухода в определенный момент p(t)

автосерфинг по тегам(показанным)
  кнопка спуск в аттрактор(локальный минимум)
  кнопка перебор подмножеств тегов(циклическим(например, зеркальным) кодом √ре€)
  кнопка случайный автосерфинг
кнопка остановить автосерфинг

флажек показать теги
  зеленый: новые
  синий: персональные попул€рные
  черный: коллективные попул€рные(те, что не вошли в персональные)